package main

import (
	"os"
	"ryuko-build/runtime"
)

func main() {

	shell := runtime.Shell{
		Stdin:  os.Stdin,
		Stdout: os.Stdout,
		Stderr: os.Stderr,

		Main: Main,
	}

	os.Exit(shell.Run())
}
